package main

import (
	_ "{{pkg}}/assets/document/statik"
	"{{pkg}}/cmd"
	"log"
)

func main() {
	log.SetFlags(log.LstdFlags | log.Lshortfile)
	if e := cmd.Execute(); e != nil {
		log.Fatalln(e)
	}
}
