Target="{{project}}"
Dir=$(cd "$(dirname $BASH_SOURCE)/.." && pwd)
Version="v0.0.1"
Platforms=(
    darwin/amd64
    windows/amd64
    linux/arm
    linux/amd64
)
UUID="{{uuid}}"
Protos=(
    system/system.proto
    session/session.proto
    user/user.proto
    logger/logger.proto
)