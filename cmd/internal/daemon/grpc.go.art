package daemon

import (
	"github.com/grpc-ecosystem/grpc-gateway/v2/runtime"
	"google.golang.org/grpc"
)

func newGRPC(mux *runtime.ServeMux, cc *grpc.ClientConn) (srv *grpc.Server) {
	var opts []grpc.ServerOption

	srv = grpc.NewServer(opts...)
	return
}
