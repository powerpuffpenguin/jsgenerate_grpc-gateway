package daemon

import (
	"net/http"

	"github.com/grpc-ecosystem/grpc-gateway/v2/runtime"
)

func httpHandle(mux *http.ServeMux, gateway *runtime.ServeMux, swagger bool) {
	mux.Handle(`/`, gateway)
}
